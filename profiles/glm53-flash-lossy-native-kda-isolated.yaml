schema: voom.runtime-profile.v1
name: glm53-flash-lossy-native-kda-isolated
description: Legacy alias for the exact SIMD-striped GLM-5.3-Flash KDA profile.
notes:
  - "Compatibility name retained for old commands. The prior scalar native kernel was lossy; it has been replaced by the D128-only, reduction-tree-matched, contraction-disabled exact kernel used by glm53-flash-lossless-striped-kda."
  - "No compact MLA, int8 K/V, fused sparse attention, coalesced expert-position GEMMs, quantization, or pruning is enabled. Prefer the canonical lossless profile name in new invocations."
extends:
  - glm53-flash-lossless-striped-kda
